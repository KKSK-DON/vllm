# int8 KV Cache 动态量化项目 · 成果报告

> 2026-08-12 步骤③收官时整理;2026-08-17 步骤④静态/物理收官更新;2026-09-13 换模型换平台复验（见第八节）。int8 kvcache 量化项目（按 Doc 执行）。
> 一~七节:分支 `KKSK-DON/vllm` `feature/int8-kvcache`；模型 Qwen3.5-9B（混合架构）；机器 RTX 4090 48G（步骤③）/ RTX PRO 6000 Blackwell 96G（aime 战役与步骤④）。
> 第八节:分支 `int8-kvcache-pretty-cc`；模型 Qwen3-30B-A3B-Instruct-2507（48/48 全注意力 MoE）；机器 8×H100 80G。

---

## 一、一句话总结

在 vLLM 中以纯 PyTorch 完整实现 int8 KV cache 量化的**三代方案**——动态（每步现场 findmax）→ 静态（写入路径校准的固定 scale）→ 物理存储（一等公民 `kv_cache_dtype="int8"`,池子真 int8）——在 humaneval / gsm8k / aime25（"25" 是年份 = AIME 2025 数学竞赛,全集 30 题）上**全线统计无损**,物理与静态**分数与生成总量双双精确一致**,长上下文同卷耗时阶梯 动态 ~60 → 静态 ~46 → 物理 ~37 分钟（原生 12）,**KV 容量 ×1.94**（64k 档,单卡并发 28→55）。

分阶段留痕:步骤③动态量化（2026-08-12）—— humaneval + gsm8k 全量三组对照,精度差异全部小于一个标准误（≤0.15pp）;步骤④静态+物理（2026-08-17）—— aime25 正赛物理与静态精确一致（0.2667/0.3333 双双复现）,详见「三、aime25 七连测终表」;**换模型换平台复验（2026-09-13,Qwen3-30B-A3B + H100）—— 全注意力架构下 KV 容量倍率从 ×1.94 升到平的 ×2.000,新增的"零量化 bf16 裁判档"把"换实现"与"加量化"两种效应分离:1319 题配对检验中,唯一 p<0.05 的差异来自换实现本身,两种量化粒度相对该参照统计无差异（p=0.28 / p=1.00）**,详见「八、第二模型 · 第二平台复验」。

背景动机（Doc 设定）：国产芯片无原生 fp8 算力 → 需要 int8 路线；KV cache 是 decode 显存与带宽的大头。

---

## 二、完成了什么（对照 Doc 五步）

| Doc 步骤 | 状态 | 产出 |
| --- | --- | --- |
| (1) kvcache 分布分析、定量化维度 | ✅ | `kvcache_distribution.py`：K 侧 outlier 呈"列状纹理"（固定 channel 跨 token 系统性偏大），per-channel 的 K 侧 MSE 低 3-4 倍 → 选定 per_channel/per_head 两粒度对比 |
| (2) PyTorch 量化 pageattention + 单测 | ✅ | `tests/kernels/attention/test_flash_attn.py`：`yang_paged_attn`（反量化先行版）+ `yang_paged_attn_int8_accelerate`（int8-first 加速版，含 per_channel 下 k_scale 吸收进 q 的数学等效技巧）。GPU 实测 **640/640 全过** |
| (3) 模型中动态量化验证精度 | ✅ **本阶段主体** | 详见下文三、四 |
| (4) 静态量化 | ✅ **完成并实测**（含物理 int8 存储,超出 Doc 要求） | 写入路径校准（aime25 末 5 题量尺,前 15 题考卷零重叠）→ `int8_static_*`（bf16 池+死尺）→ `int8_phys_*`（一等公民 `kv_cache_dtype=int8`,池子物理 int8）。四场 gsm8k 冒烟全部 0.875 与基线逐题持平;**aime25 正赛四场收官:物理与静态分数精确一致,耗时 动态≈60→静态≈46→物理≈37 分钟**;KV 容量实测 8k 档 ×1.73、**64k 档 ×1.94**——终表见「三、aime25 七连测终表」 |
| (5) 真 kernel 性能优化 | 可外包（文档原文"假装外包"） | 未做；aime 平方成本观察为其必要性提供了实证（见 4.3） |

### 步骤③的具体工程

1. **接线**：`flash_attn.py` forward 非 DCP 分支加 `YANG_ATTN_MODE` 环境变量岔路口（bf16 / int8_per_channel / int8_per_head 三档）→ 新文件 `yang_attn.py`。
2. **适配器四件套**：query_start_loc 前缀和差分出 query_lens；seq_lens/block_table 直取；现场量化；`copy_` + `view_as` 写回调用方预分配的输出缓冲（out-parameter 模式，CUDA graph 地址稳定性所需）。
3. **围栏**：8 道 assert（alibi/sinks/causal/sliding_window/fp8/mm-prefix/R-SWA/token 数一致性）——不理解的分支不硬啃，用 assert 拦住防静默走错路；激活日志（每模式打一次）保证"确实走了我的代码"可证。
4. **验证梯子**：先 bf16 纯 PyTorch 等效版与原生 flash 内核同题对照（8 题 gsm8k **逐题一致** 0.875 = 0.875）证明管道，再切 int8 证明量化数学。
5. **两轮迭代**（见四）：NaN 修复 → 提速 6.5 倍。

代码提交：`491e48c21`（接线 + NaN 修复）、`a728143de`（提速 + fp32 除法）。

---

## 三、最终数据

### 三组对照总表

| 基准 | 原始 bf16 | int8_per_channel | int8_per_head |
| --- | --- | --- | --- |
| **humaneval** pass@1（164 题，0-shot） | 0.7073 | **0.7134**（+0.6pp） | **0.7073**（±0） |
| **gsm8k** strict（1319 题全量，5-shot） | 0.8787 | **0.8772**（−0.15pp） | **0.8779**（−0.08pp） |
| gsm8k flexible | 0.8741 | 0.8704 | 0.8741 |
| aime25（15 题半卷 + \boxed 指令,0-shot） | 0.2667 (4/15) | 0.2000 (3/15) | 0.3333 (5/15) |

\* aime25 二次战役后补齐：首轮基线 0 分,经逐样本取证发现模型三题全对而判分器全判零——判分正则只认「回复即答案」或 \boxed{} 两种信封,长推理散文两样都没有。加系统提示「final answer within \boxed{}」后判分链路打通。最终三组差异 ±1 题,双向摆动,均在误差棒（±0.13）内。per_head 高于基线属小样本噪声,非量化增益。生成上限 20480 token（实测最长链 ~14k,留 40% 余量）。

### aime25 七连测终表（七种实现同卷对决,2026-08-17 静态/物理收官）

同机（RTX PRO 6000 Blackwell 96G）同卷（aime25 前 15 题,chat template + \boxed 系统提示,`max_gen_toks=20480`,`enforce_eager`）:

| 实现 | 耗时 | 生成吞吐(15 路合计) | 实际生成总 token | exact_match |
| --- | --- | --- | --- | --- |
| bf16 原生融合内核 | 11 分 54 秒 | 465.8 toks/s | ≈277.6k | 0.2667 |
| `int8_per_channel`（动态,每步现场 findmax） | 59 分 36 秒 | 79.0 | ≈273.2k | 0.2000 |
| `int8_per_head`（动态） | 56 分 31 秒 | 83.4 | ≈273.1k | 0.3333 |
| `int8_static_per_channel`（bf16 池+静态尺） | 47 分 54 秒 | 101.1 | ≈278.7k | 0.2667 |
| `int8_static_per_head` | 44 分 47 秒 | 103.6 | ≈266.1k | 0.3333 |
| `int8_phys_per_channel`（**池子物理 int8**） | **37 分 47 秒** | **129.5** | ≈278.6k | 0.2667 |
| `int8_phys_per_head` | **36 分 47 秒** | **127.2** | ≈266.1k | 0.3333 |

\* 公平性说明:七场都是同一份考卷（aime25 前 15 题,各场 results json 均记 `"limit": 15`）,且**实际生成总量几乎相同（266k-279k,±2.4%）**——耗时差异是纯粹的每 token 速度差,不存在"哪场题少或生成短"的水分。吞吐为 15 路并发合计（原生单路平均 ≈31 toks/s）;「耗时」含 ~2 分钟引擎启动,吞吐只计生成段。另:物理与静态同粒度的总生成量差 <0.01%（pc 278,641 vs 278,659;ph 266,082 vs 266,071）——「同尺同舍入 → 同轨迹」等价性的又一实证。

三条硬结论:

1. **精度**:物理与静态逐格同分（0.2667/0.3333）,与等价性推演一致——同一把 scale 同一次舍入,"写入时量化一次"与"读取时每步量化"的反量化值应逐位相同,贪心解码下轨迹相同、分数必然复现。静态 per-channel 与 bf16 基线同分:校准数据（aime 末 5 题）与考卷（前 15 题）零重叠,长上下文零掉分。
2. **耗时三层拆解**（看 per-channel 列）:动态→静态 **−11:42** = 免掉每步对收集到的 KV 现场 findmax;静态→物理 **−10:07** = 收集搬运字节减半（int8 池 vs bf16 池）+ 读取侧免量化（码已是 int8,`pre_quantized=True` 直接乘 scale）;物理→原生剩 **~26 分钟** = python 逐步 gather + fp32 einsum 的模拟开销,即步骤⑤融合 kernel 的全部标的。
3. **4.3 的平方成本观察被定量补全**:当年同根因让 32k 动态场跑 3.5 小时,今天在 ~20k 生成档位表现为动态比静态多付 ~12 分钟——"每步统计"从账单上划掉后,省下的实测价格与理论方向完全一致。

补测（2026-08-19,chunked prefill 真切块对拍,详录见 `KV_CACHE_QUANTIZATION.md` 附录 F.6）:10k 提示切成 ~10 块下,yang bf16 / 静态 / 物理三者输出**逐字节相同**,且 yang 实现对块边界**逐字节不变**;五路对拍中唯一漂移来自原生融合内核自身的切块浮点抖动（近平局差一个词）。切块读写正确性收官;顺带测得 16k 档容量 ×1.842,与 8k ×1.73、64k ×1.94 构成单调逼近 2× 的三点曲线。同日补测 prefix caching(混合架构须显式开启):原生/静态/物理三模式**开关缓存输出逐字相同**,开缓存后共享前缀第二问提速 32-42%(命中证据)——物理模式的 int8 块被跨请求复用零误差,"全局时不变 scale ⇒ 块可共享"实测成立,详见附录 F.7。

- **所有差异 ≤0.6pp，小于一个标准误（±0.9pp）**——正确表述是"量化版与原版的差异小于随机波动"，不是"掉得少"。
- **per_channel 没有赢 per_head**：分布分析的 MSE 优势是真的，但 Qwen3.5-9B 的 outlier 病情未重到让 per_head 的 128 档刻度不够用——**量化方案的收益取决于数据分布的病重程度**；拉开差距需换 outlier 更凶的模型或降到 int4。
- 复现性：克隆实例上全量 gsm8k 基线 0.8787 vs 原机 0.8749（±0.9pp 内）；bf16 冒烟与原生逐题一致。
- **八连平**（同卷 gsm8k 8 题,变量逐一引入）：原生 / bf16 / 动态 pc / 动态 ph / 静态 pc / 静态 ph / 物理 pc / 物理 ph 全部 0.875——每个新变量（换算子→动态量化→死尺→物理存储）单独证明清白。静态尺以 aime25 末 5 题校准、考 gsm8k 仍持平,跨领域泛化成立。
- 速度（8 题 gsm8k 生成段）：原生 8s ≈ bf16 版 9s ≈ 动态 int8 版 9s（首版整池量化 59s）≈ 静态版 9s ≈ 物理版 8-11s（输出 81-99 toks/s）——短上下文下六种实现同速;静态/物理的结构性优势（免每步 findmax,线性 vs 平方）在长上下文实测显形:动态 59:36 → 静态 47:54 → 物理 37:47（原生 11:54）,详见「aime25 七连测终表」。
- **显存容量（c8 实测,物理版 vs auto,同机同 58.59 GiB 预算同 max_model_len=8192）**：KV cache 总容量 1,567,690 → **2,707,828 token（×1.727）**,最大并发 191.37× → **330.55×**。未达理论 2× 的原因是混合架构:8 层全注意力的每 token 字节减半,24 层 GDN 线性注意力的状态页不随 kv_cache_dtype 变,共享池摊平后得 1.73×（由此可反推 GDN 状态摊销占地 ≈ 注意力 bf16 占地的 19%）;纯 Transformer 架构下该比值应逼近 1.94-2.0×。**64k 档位实测印证了这一预测:1,861,632 → 3,610,437 token（×1.939）,并发 28.41x → 55.09x**——GDN 状态按序列数计费（每序列定长,与上下文无关）而注意力 KV 按 token 数计费,上下文档位越长、单序列 token 越多,GDN 占比被摊得越稀,比值就越逼近理论 2×。

---

## 四、三次事故 → 三个发现（复盘素材）

### 4.1 NaN 事故：整池 findmax 读到无主内存

- **现象**：两种 int8 首跑全部 0/8，而单测 640 用例全过、bf16 模式满分。
- **取证**：探针打印显示第一次调用时"本批引用块 absmax=13.31（健康）、整池 absmax=NaN"；从第二次调用起引用块也 NaN——毒素沿"注意力输出→隐藏状态→写回 cache"逐层传染。
- **定罪**：混合架构模型所有层共用一块打包大内存（vLLM `attn_utils.py` 注释原话 "all packed tensors alias the same backing"），本层视角的"池子"横跨别层地盘；bf16 视角读任意字节必然撞上 NaN 位模式。
- **修法**：scale 统计范围从整池缩到"本批引用块的合法 token"（逐序列 gather + 掐掉最后一块未写满的尾巴——尾巴同样是无主字节）。
- **教训**：动态量化的统计范围 = 实际拥有的数据，一个字节都不能多。vLLM 自家 fp8 从构造上免疫（写入时量化 + 离线校准静态 scale，从不扫池）。传统单体架构模型上此 bug 不炸但仍错（过期数据默默撑大 scale）——Qwen3.5 的共享内存布局把静默错误放大成响亮失败，反而是帮忙。

### 4.2 bf16 除法掉一题：量化算术的中间精度是实变量

- 提速版把量化除法从 fp32 降到 bf16（bf16 尾数仅 8 位，取整前的商偶尔差一档），per_channel 8 题回归从 0.875 掉到 0.75。两版数学等价、种子固定，唯一变量就是除法精度——干净的 A/B 归因。抬回 fp32 后归位。
- **教训**：round 之前的算术在 fp32 里做；"数学等价"不等于"数值等价"。

### 4.3 aime 平方成本：读取侧动态量化的固有缺陷现形

- 32k 上下文下 int8 场次 GPU 99% 利用率却需 3.5h/场（基线 21min）：每生成一个 token 都要把该序列全部已有 KV 重新 gather + findmax + 量化，单步成本随上下文线性涨、整场平方级。gsm8k 的 1.4k 上下文把它藏住，32k 让它现形。
- **教训**：这正是工业界选"写入时量化（每值一生只处理一次）+ 静态 scale + 融合 kernel"的根本动机。实验数据完整走通了这条因果链——从"为什么我的方案慢"推出"为什么 vLLM 那样设计"。

---

## 五、关键设计决策记录

| 决策点 | 选择 | 理由 |
| --- | --- | --- |
| 量化位置 | 读取侧算子替换（pageattention 调用处），非写入侧 | Doc 步骤③原文；手术范围最小，精度实验最快出数 |
| scale 统计范围 | 本批引用块的合法 token（token 级精确，掐块尾） | NaN 事故教训；块级不够（尾巴是无主字节） |
| 统计与量化分离 | `compute_kv_scale` 只算 scale；量化在注意力循环内对 gather 出的 token 做 | 统计可分块合并（max 结合律），变换必须全局一次；避免整池除法的 59s 开销 |
| 量化对象 | 每序列 gather 出的干净 K/V（全局 scale 传入） | 与"先整池量化再 gather"逐位等价（gather 与逐元素运算可交换），且天然绕开无主内存 |
| 除法精度 | fp32 | 4.2 的 A/B 证据 |
| 冻结已验证算子 | `yang_paged_attn_int8_accelerate` 尽量不动，脏活留给适配层 | 640 用例的"已验证"标签最贵；环境问题归适配器管，数学层假设输入干净 |
| per-seq scale（更细粒度） | 讨论后未采用 | 精度会更好但偏离单测与文档定义的"全局 scale"算法，实验可比性优先；作为取舍话题保留 |
| cascade attention | 不处理 | 本版本默认关闭（`disable_cascade_attn=True` 需 opt-in），gate 不会被绕过——从总开关向下查证,而非从触发条件向上猜 |

---

## 六、与工业方案（vLLM fp8）的对照

| 环节 | 本项目（动态 int8） | vLLM fp8 生产路线 |
| --- | --- | --- |
| scale 何时定 | 每次调用现场 findmax | 部署前离线校准，随权重发货（`layer._k_scale`） |
| 数据何时量化 | 每次调用重新量化引用到的 token | 每 token 写入 cache 那一刻量化一次，终身不改 |
| cache 存储格式 | 仍 bf16（临时 int8 副本，不省显存） | 物理 fp8，容量翻倍是真的 |
| 注意力算子 | 纯消费者：吃 int8 + scale | 纯消费者：吃 fp8 + descale（同一接口思想） |
| 长上下文成本 | 平方级（4.3） | 常数级 |

三级台阶总结：**正确性从来不需要 kernel（纯 PyTorch 全程可达）；真省显存需要 int8 存储格式（仍可纯 PyTorch，慢）；kernel 买的只是速度**。标准开发顺序：纯 PyTorch 版当标准答案 → kernel 版对拍。本项目的 PyTorch 实现即未来 kernel 的对拍基准。

---

## 七、遗留与可选项

1. **成果条目提炼**（按 Doc 的收尾流程）——下一个动作。
2. ~~静态量化~~ ✅ 已完成（2026-08-17）：写入路径校准 + 静态尺读取 + 物理 int8 存储全链路实测,见「三、aime25 七连测终表」。
3. ~~aime 三组对照补齐~~ ✅ 已完成（2026-08-13 动态三组带 chat template + 2026-08-17 静态/物理四组）。
4. 逐题错误分析：需带 `--log_samples` 重跑（当时未加，逐题输出不存在），需要深挖时再做。
5. CUDA kernel（④a 写入内核起步）：当练手场，Doc 未作要求。

## 八、第二模型 · 第二平台复验（Qwen3-30B-A3B-Instruct-2507 + H100，2026-09-13）

> 分支 `int8-kvcache-pretty-cc`（可读性重构版,静态模式已由 `int8_phys_*` 更名为 `int8_static_*`,原"校准尺+bf16 池"模拟档已删除）；机器 Dell XE9680 8×H100 80G（共享,本项目只占 4-7 号卡）；工作区 `/data/ellm/yang/yang1`（与同机其他项目完全隔离）。
>
> 换模型的动机:Qwen3.5-9B 是混合架构,32 层里只有 8 层带 KV cache,容量收益被线性注意力层的定长状态稀释（×1.73~1.94,够不着理论 2×）。Qwen3-30B-A3B 是 **48/48 全注意力**的 MoE（`sliding_window: None`）,单卡装得下,没有稀释项。

### 8.1 精度:三张考卷 × 四档实现

新增 **bf16 裁判档**（同一份 PyTorch 注意力,零量化）作为中间参照——没有它,"原生 vs int8"的差异分不清是"换实现"还是"加量化"造成的。

| 考卷 | 原生融合内核 | bf16（本实现,零量化） | `int8_static_per_channel` | `int8_static_per_head` |
| --- | --- | --- | --- | --- |
| **gsm8k** strict（1319 题全量,5-shot） | 0.8795 (1160) | **0.8696 (1147)** | 0.8757 (1155) | 0.8704 (1148) |
| **humaneval** pass@1（164 题,0-shot） | 0.7683 (126) | **0.7683 (126)** | 0.7622 (125) | 0.8049 (132) |
| **aime25**（15 题半卷 + \boxed 系统提示） | 0.4667 (7/15) | — | **0.4667 (7/15)** | **0.4667 (7/15)** |

aime25 三档**精确同分**,且原生基线 0.4667 远离天花板与地板（9B 时代只有 0.2667,掉分空间小得多）——信号空间充足而两种粒度都没掉。

### 8.2 配对分析:把"换实现"和"加量化"分开

三档答的是**同一批题**,所以独立样本的标准误是错的尺子;正确判据是**两个方向各翻转多少题**（McNemar 精确检验）。

gsm8k 全量 1319 题:

| 比较 | 输出不同 | 左对右错 | 左错右对 | McNemar p |
| --- | --- | --- | --- | --- |
| 原生 vs **bf16（零量化）** | 644 | 23 | 10 | **0.0351** |
| 原生 vs per_channel | 820 | 23 | 18 | 0.5327 |
| 原生 vs per_head | 1042 | 38 | 26 | 0.1686 |
| **bf16 vs per_channel** | 824 | 17 | 25 | **0.2800** |
| **bf16 vs per_head** | 1035 | 30 | 31 | **1.0000** |

**全表唯一达到 p<0.05 的差异是"换实现"本身,与量化无关。** 用正确的参照（bf16,同实现零量化）衡量,两种粒度的 int8 量化都统计无差异（p=0.28 与 p=1.00,后者是 30 比 31 的完美平衡）。

humaneval 上 per_head 的 0.8049（比原生高 6 题）曾引起怀疑,三条证据将其归位为噪声:

1. **零量化的 bf16 档与原生分数完全相同（126/126），但 45/164 道题写出了不同的代码**——这就是"换实现"的扰动地板,量化只是在它之上再加（per_channel +14 题分歧,per_head +26 题）。
2. **判分器清白**:10 道翻转题里代码逐字节相同的有 **0** 道（humaneval 要真执行生成代码,高负载下超时抖动是首要嫌疑,已排除）。
3. **三张考卷横看无方向**:per_head 在 aime 与原生同分、在 gsm8k 低于原生、在 humaneval 高于原生——忽高忽低正是噪声特征。翻转题里输方的输出平均长 3 倍（1054 vs 359 字符）、重复率高 3 倍（0.10 vs 0.03）,第 109 题原生写出 4407 字符、75% 重复行,是贪心解码陷入复读的典型——**翻转集中在模型本就在"写对"与"复读"之间摇摆的题上**。

### 8.3 KV 容量:精确 ×2,三档全中

| 上下文档位 | bf16 池 | int8 池 | 倍率 | 最大并发 |
| --- | --- | --- | --- | --- |
| 8k | 74,512 token | 149,024 | **2.000** | 9.10× → 18.19× |
| 16k | 74,512 | 149,024 | **2.000** | 4.55× → 9.10× |
| 64k | 74,384 | 148,768 | **2.000** | 1.14× → **2.27×** |

9B 的爬升曲线（1.727 → 1.842 → 1.939）在这里变成**一条平的 2.000 直线**——全注意力架构没有"按序列计费的定长状态"这一稀释项,理论极限直接吃满。账也对得上:48 层 × 2（K/V）× 4 KV 头 × 128 维 × 2 字节 = 96 KiB/token,74,512 × 96 KiB = 6.82 GiB,与引擎报告的 KV 显存一字不差。

**64k 档的质变**:bf16 并发 1.14 意味着一次只装得下一条满长请求,int8 的 2.27 意味着能真正并行两条——服务能力的性质变化,不只是数字变大。另一处质变是前缀缓存命中率,见 8.5。

顺带厘清一个容易说反的点:块大小两边都是 16 token（74,512 与 149,024 均能被 16 整除,块数 4657 → 9314）。int8 **不改变单块装几个 token**,改变的是单块占几个字节（3072 → 1536）,于是同样显存装得下两倍数量的块。

### 8.4 速度:静态 vs 动态

固定负载（批量 32,提示 1050 token 互不共享前缀,贪心生成 128 步,关前缀缓存,串行独占单卡）。预填充与解码分开计时:先测 `max_tokens=1`（得预填充）,再测 `max_tokens=129`,差值 ÷128 得每步解码成本。

| 实现 | 预填充 | 每步解码 | 相对原生 |
| --- | --- | --- | --- |
| 原生融合内核 | 0.64s | **42.1 ms** | 1.0× |
| bf16（本实现,零量化） | 1.77s | 372.7 ms | 8.9× |
| `int8_static_per_channel` | 2.38s | 567.1 ms | 13.5× |
| `int8_static_per_head` | 2.57s | 558.4 ms | 13.3× |
| `int8_per_channel`（动态） | 2.53s | **732.9 ms** | 17.4× |
| `int8_per_head`（动态） | 2.76s | **756.5 ms** | 18.0× |

**静态比动态每步快 23%~26%**（per_channel 567.1 vs 732.9 ms;per_head 558.4 vs 756.5 ms）——省下的正是"每步重新扫描已引用 KV 求最大值"这笔账。注意这个尺度**严重低估**动态的劣势:动态成本随上下文长度增长,1050 token 只看到 23%,9B 时代 32k 上下文的 aime 场动态跑了 3.5 小时而原生只要 21 分钟。

零量化的 bf16 档就已经比原生慢 8.9 倍,**这 330 毫秒/步的差额全部是 PyTorch 参考实现的固有开销,与量化无关**——也就是步骤⑤融合 kernel 的全部标的。

### 8.5 调度特性复验

**切块预填充**:三种实现 × 切块（单步预算 512）/ 不切块（8192）共六场,**生成文本全部逐字节相同**（9B 那次原生尚有一词漂移,这次连那点都没有）。见证日志确认切块真的发生:4765 token 被切成 9 整块 + 157 的零头,每块都经本算子。

**前缀缓存**:双判据均通过。①第一问答案在 6 场中逐字节全同,第二问答案句亦全同;②第二问耗时 原生 0.70→0.67s（4%）、bf16 1.65→1.03s（**38%**）、静态 int8 2.16→1.32s（**39%**）——int8 存储的缓存块跨请求复用零误差。答案之后的自由续写尾巴在开/关缓存间有分歧,但**原生与 int8 表现完全一致**（同一条件下选同一种写法）,故归因于缓存路径本身的浮点顺序,与量化无关。

**命中率实测（2026-09-13 补测）**:容量翻倍是否真能换来更高的前缀缓存命中率,此前只是机制推理。实测方法——造 32 段互不相同的前缀（每段 2863 token,合计 91,616）,一轮发满填充缓存,二轮同序再发一遍,读 vLLM 自带的 `vllm:prefix_cache_queries` / `vllm:prefix_cache_hits` 计数器（单位是 token 数）。工作集刻意卡在两个池容量之间:

| 池 | 池容量 | 工作集 | 工作集 / 池容量 | 第二轮命中率 |
| --- | --- | --- | --- | --- |
| bf16 | 74,512 | 45,808（16 段,对照组） | 0.61× | **99.9%** |
| bf16 | 74,512 | 91,616（32 段） | 1.23× | **0.0%** |
| int8 | 149,024 | 91,616（32 段） | 0.61× | **99.8%** |

三点连起来给出的是**门槛关系,不是比例关系**:工作集小于池容量 → 命中率接近满;越过池容量 → 在"最久未用先淘汰"策略下轮流访问是最坏情况,**每段前缀恰好在轮到它之前被挤出,命中率塌到 0**。int8 的作用是把同一个工作集从"1.23 倍容量"挪到"0.61 倍容量",即**把负载从门槛外搬进门槛内**。

第一行是必需的对照:没有它,0.0% 可以被解释成"原生这边前缀缓存根本没启用";有了 99.9%,才能确定 0.0% 纯粹由容量不足导致。

边界:轮流访问是该淘汰策略的最坏模式,真实负载不会这么齐整,所以 0% vs 99.8% 是差距的**上界**而非典型值。另有一个与命中率无关的独立收益未单独量化:池子更大时调度器更少需要**抢占**正在运行的请求（`vllm/v1/core/sched/scheduler.py` 分配失败时踢出运行中请求并稍后重算）——那损失的是已完成的工作,与"未命中"是两种不同的浪费。

### 8.6 H100 平台三个新发现（9B/Ada 时代未遇）

| 发现 | 现象 | 处理 | 意义 |
| --- | --- | --- | --- |
| **FA3 提前排程拒收 int8** | Hopper 默认走 FlashAttention v3,其启动期的 `mha_fwd_get_scheduler_metadata` 校验 KV 数据类型,int8 不在白名单,引擎起不来 | 钉 `flash_attn_version=2`（FA2 无此预排程步骤） | **"int8 走非量化车道、原生消费者大声失败"的设计预言成真**:失败发生在启动期、指名道姓、无静默算错的机会 |
| **CUDA 图捕获与本实现互斥** | `yang_forward` 把长度读回主机（`.tolist()`）再按长度做 python 循环;图捕获要求全程纯 GPU 命令且形状静态 | 全场 `enforce_eager=True` | 症状是那一行读回,**病根是"形状随数据变"**;要可录制须改成补齐+掩码的静态形状实现,代价是补齐浪费 |
| **PyTorch 参考的平方内存墙** | 一次性预填充 7616 token 时需摆出 [32 query 头, 7616, 7616] 的分数表 = 6.92 GiB,加在 61 GiB 权重上显存不足（切块档只需 0.93 GiB,原生融合内核从不摆出该表） | 探针提示缩到 4765 token | **kernel 必要性的第三条论据**:不只是慢,还有随长度平方增长的内存墙 |

### 8.7 其他

- **单测**:`pytest tests/kernels/attention/test_flash_attn.py -k q_dtype2` → **640/640 全过**（117 秒,H100 单卡）。这是 `int8-kvcache-pretty-cc` 分支上任何代码的首次 GPU 运行——此前三个重构提交只有 CPU 逐位对拍与静态检查背书。
- **校准账本**:48 层 × 8 表齐整;K 的逐层最大值 **14.19 ~ 158.00**（中位 28.75）,V 为 0.12 ~ 15.12。9B 的 K 只有 12.50~18.25——新模型离群值凶得多,per_channel 的必要性因此更突出（per_head 只有 4 把尺,一个离群通道就会拖粗整头的量化步长）。
- **档案**:`~/Documents/personal-projects/vllm-eval-archive/h100-qwen30b/`（evals 逐题 JSON + 容量/兼容性/速度日志 + 驱动脚本 + 校准产物,58 MB,离线可查）。

---

## 九、档案索引

- 代码：`feature/int8-kvcache` 分支，commits `491e48c21`、`a728143de`（步骤③）、`d49dce793`（步骤④静态+物理整包:校准观察员/静态尺读写/一等公民 `kv_cache_dtype=int8`）、`302a755c8`（校准脚本兼容 lm-eval 的 `!function` yaml 标签）；核心文件 `vllm/v1/attention/backends/yang_attn.py` + `flash_attn.py` 岔路口；单测 `tests/kernels/attention/test_flash_attn.py`；校准脚本 `yang_calibrate_kv.py`。
- 评测原始日志/JSON：gpuhub 机器数据盘 `/root/autodl-tmp/evals/{A_*,B_*,C_*,D_*,E_*,F_*,V_*}` + 汇总 `AIME_FINAL_SUMMARY.txt` + 校准产物 `kv_scales.pt`（关机保数据）；**本地全量镜像 `~/Documents/personal-projects/vllm-eval-archive/`**（离线可查,免开机器）。
- 技术笔记：`KV_CACHE_QUANTIZATION.md`（附录 E 为本阶段实验详录；D.5 为量化粒度判读方法）。
- 工作方式备注：代码由本人手写（两轮重写：接线适配器、scale 重构），AI 担任评审（严重度排序错误清单）、运维（远程机器与评测编排）与教学讲解；诊断结论均经探针数据或源码原文验证。
