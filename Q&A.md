# Q&A — 项目复盘问答集

> 复盘 int8 KV cache 项目时的疑问与解答,按问答原文整理,持续追加。
> 术语约定见 `KV_CACHE_QUANTIZATION.md` 附录 B 术语表(码、尺等)。

---

## Q1: `blk_idxs = block_tables[i, :num_kv_blocks]` 为什么能拿到块编号?怎么知道这样就能拿到?

先说这个张量是谁、谁填的、长什么样:

- **`block_table` 是调度器每一步发给注意力层的元数据之一**,形状 `[本批序列数, 上限块数]` 的二维整数张量。
- **谁填的**:vLLM 的 KV cache 管理器。每当第 i 条序列需要新空间(现有块写满了),管理器从空闲块清单(记录哪些物理块当前没人用)里取一个块,把它的编号**追加**到第 i 条序列的列表末尾。因为是按需追加,所以**列表的顺序 = token 的先后顺序**:第 j 个位置的编号,就是"这条序列第 j×block_size 到第 (j+1)×block_size−1 号 token 存放的物理块"。
- **为什么第 i 行对应我的第 i 条序列**:同一步的所有元数据(`query_start_loc` 的第 i 段、`seq_lens[i]`、`block_table` 第 i 行)是调度器**一起构造**的,行序一致——这是 vLLM 的接口约定。
- **为什么要切 `[:num_kv_blocks]`**:所有序列共用一个矩形张量,行宽按最长的序列设,短序列的行**尾部是填充值,不是它的块**。`num_kv_blocks = ceil(kv_len / block_size)` 算出这条序列真正占了几个块,只取前这么多个。

拿到编号后,`x[blk_idxs]` 是 pytorch 的整数数组索引:cache 张量第 0 维就是物理块编号,给它一串编号,它按你给的顺序把这些块取出来拼在一起——于是"物理上散落"的块,取出来后恰好是"逻辑上连续"的 token 序列,再 `[:kv_len]` 切掉最后一块没写满的部分。

### 关键陷阱:逻辑块号 ≠ 物理块号(必须查表翻译)

直觉写法"`token 位置 // block_size` 定位到哪个块,直接去池子取"是错的:除法算出的是**这条序列自己数的第几块**(逻辑块号),而池子第 0 维编号的是**全池统一的仓位号**(物理块号),两者不相等,中间必须查一次 `block_table` 换算。

具体例子:块大小 = 4 个 token,池子 10 个物理块,`key_cache` 形状 `[10, 4, 4, 256]`(物理块数 × 每块座位 × KV 头数 × 头维)。序列 A、B 同时在跑,块是谁先要谁先领:

```text
A 领到物理块 0(存 A 的 t0-t3)
B 领到物理块 1(存 B 的 t0-t3)
A 写满了,再领物理块 2(存 A 的 t4-t7)
B 再领物理块 3(存 B 的 t4-t5,没写满)
A 再领物理块 4(存 A 的 t8-t9,没写满)

block_table 第 A 行 = [0, 2, 4, 填充, …]
block_table 第 B 行 = [1, 3, 填充, …]
```

问:A 的第 6 个 token(t6)在哪?错误算法:`6 // 4 = 1`,直接去物理块 1 → 那里躺的是 B 的数据。正确三步:

```text
① 逻辑坐标:  逻辑块号 L = 6 // 4 = 1     块内偏移 o = 6 % 4 = 2
② 翻译:      物理块号 P = block_table[A][L] = block_table[A][1] = 2
③ 取数:      key_cache[2, 2]  →  A 的 t6 的 K,形状 [4, 256]
```

整段收集则是:`key_cache[[0, 2, 4]]` → 形状 `[3, 4, 4, 256]`(三个物理块按逻辑顺序摞起) → `.view(12, 4, 256)`(12 个座位,已是逻辑顺序 t0…t11) → `[:10]` 切尾。注意 `block_table`(几十个整数的小张量)和池子(几 GB 的大张量)是**两个张量**:读表只得到整数编号,拿编号去大张量取数据是第二次索引。

自测:B 的 t5 在哪? 答:`L = 5//4 = 1, o = 5%4 = 1, P = block_table[B][1] = 3` → `key_cache[3, 1]`。报坐标时说"偏移 1",不说"第几个"——0 基编号(从 0 数)与日常计数(从 1 数)的歧义由此消灭。

延伸要点:判断"这段显存归谁"只需要两份凭证——写入凭 `slot_mapping`(本步每个新 token 的精确落位),读取凭 `block_table` 前 `num_kv_blocks` 项 + `kv_len` 截断。NaN 事故的修复,本质就是让 scale 统计代码也按这两份凭证访问,不多读一个字节。

---

## Q2: 为什么写入的时候不挨个连续地写?为什么要散落到各个块?

未知的不是提示词多长(提示词到达时已分词,长度已知),**而是这条序列将来会生成多少 token**——没人知道它会写 50 个还是 20000 个。

两种方案对比:

**方案甲(连续预留)**:每条序列到达时划一整段连续显存。划多大?按最大可能(如 65536 token)划,则绝大多数早早结束的请求浪费 95% 以上的预留;按估计值划,超了就要整段搬家复制。另外各序列结束时间不同,释放后留下大小不一的空洞,新请求的需求量对不上空洞大小——显存明明有余量却塞不进新请求。

**方案乙(vLLM 的 PagedAttention,分页注意力)**:把整个池子切成**等大的块**,序列**按需逐块领取**——写满一块,再从空闲块清单里领下一块,领到哪块是哪块。因为所有块等大,任何空闲块都能满足任何请求,不存在"对不上大小"的浪费;序列结束,它的块立刻回收复用。这套设计直接对应操作系统的虚拟内存分页,`block_table` 的角色等同于页表(记录"逻辑第几页对应物理哪一页"的对照表)。

所以"凌乱"要说准确:**块与块之间物理上不连续,但块内部是严格挨个写的**——`slot_mapping`(每个新 token 的写入位置编号 = 物理块号 × 块大小 + 块内偏移)保证新 token 恰好写在当前块已写内容的下一格。写入侧内核不查 `block_table`,因为调度器提前把逻辑→物理的翻译做完了,直接发物理座位号;读取侧必须查表,因为它要重建整段历史。散落是"按需增长、零浪费"的代价,而读取靠一次整数数组索引就把散落的块按序拼回,代价很小。

延伸要点:块大小是权衡旋钮——块越大,每序列平均浪费越多(最后一块平均半空),但块表越短、管理开销越小。vLLM 默认 16;Qwen3.5-9B 混合架构上被"注意力页 ≥ GDN 状态页"的对齐要求抬到 528(int8 池下 1056)——同一个参数,约束来自两个完全不同的方向。

---

## Q3: bf16 除法为什么掉了一题?(0.875 → 0.75 事故)

提速版曾把量化除法放在 bf16 里做,per_channel 的 gsm8k 8 题回归从 0.875 掉到 0.75。机制三句话:

1. 量化 = `round(v / 尺)`(尺 = scale,量化刻度)。bf16 的尾数只有 8 位有效精度(约 2-3 位十进制),商偶尔差那么一点点。
2. 差一点点为什么致命:**`round` 把连续的小误差放大成一整档的跳变**。真商是 36.4998,fp32(float32,单精度浮点,尾数 24 位)算出 36.4998 → round 成 36;bf16 精度不够,把它表示到 36.5 附近 → round 成 37。码(量化后存的 int8 整数)差一档,个别注意力分数随之偏移,在贪心解码的连锁里翻掉了一道题。
3. 除法抬回 fp32 后 0.875 归位。这是一次干净的 A/B:两版数学结构相同、种子相同,**唯一变量是除法精度**,归因无可辩驳。

教训:**round 之前的算术要在 fp32 里做;"数学等价"不保证"数值等价"。** 修复落点在代码里清晰可见——`yang_attn.py` 循环内量化写作 `torch.round(k.float() / k_scale.float())`,两个操作数都显式抬到 fp32;`compute_kv_scale` 里的统计也是 `.abs().float().amax(...)`。此后写入量化(`yang_static_write`)、动态/静态读取量化,除法全部 fp32。

对照:round **之后**的乘法(反量化,码 × 尺)用 bf16 就没事——那一侧没有跳变放大器,误差是平滑的、不会被整档放大。同一对操作,一边必须 fp32、一边 bf16 随意,分界线就是 round。
